import { Bell } from "lucide-react";
import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

function Notifications() {
  return (
    <PlaceholderPage
      icon={Bell}
      title="Notifications"
      description="Real-time alerts on new scheme matches, application status changes, and upcoming deadlines will land here — so nothing you're eligible for slips through."
    />
  );
}

export default Notifications;
