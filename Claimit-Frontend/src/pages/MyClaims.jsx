import { ClipboardList } from "lucide-react";
import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

function MyClaims() {
  return (
    <PlaceholderPage
      icon={ClipboardList}
      title="My Claims"
      description="Track every application you submit — status, required documents and next steps, all in one clear timeline. This is coming in a future sprint."
    />
  );
}

export default MyClaims;
